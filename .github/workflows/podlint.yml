name: PodLint

on:
  push:
    branches: 
      - develop
  pull_request:

jobs:
  lint:
    name: Pod Lib Lint
    runs-on: macos-12
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
      - name: Run
        run: pod lib lint